# 后续计划

已确认的视觉方向：克制、合理信息密度、系统字体、明暗配色，个人介绍优先。

- [x] 原生 Hexo 模板和基础构建验证
- [x] 公开源码、MIT、语义化版本及变更记录
- [x] Mermaid 按需本地加载与深浅色兼容
- [x] 数学公式、脚注的渲染方案与回归样例
- [ ] 图片、视频、宽表、长代码等完整正文样例
- [ ] 现有博客全量文章、图片路径和旧语法兼容检查
- [ ] 原生主题桌面、手机及 Chrome/Edge/Firefox/Safari 验收
- [ ] 键盘操作、对比度、放大、打印与资源失败检查
- [x] 搜索性能与核心资源体积测量
- [ ] 完整安装、升级、回退验证后发布稳定版本

## 2026-09-08 续接复验

- 图片懒加载开启后，重新执行博客 `npm run clean`、`npm run build`，构建通过。
- 全站静态审计通过：268 个 HTML 页面、3946 个内部引用，无缺失的本地引用，无缺少 alt 属性或懒加载属性的图片。外部图片实际可用性和 alt 文本质量不属于本项结论。
- 1 篇 Mermaid 文章生成对应图表容器及本地脚本引用；实际文章未检测到公式和脚注，其渲染由原创测试样例覆盖。
- 修复临时测试站点对博客根目录依赖的解析后，主题默认 `npm test` 的 15 项检查通过。
- CSS 9967 字节，主脚本 5599 字节，搜索索引 276630 字节；Mermaid 包 3572661 字节，仅图表页面引用。
- 上一任务记录了 Chromium 桌面、手机、明暗切换、Mermaid、搜索和键盘焦点检查；本次续接未重复浏览器验收。跨浏览器、完整媒体、打印、资源失败以及安装升级回退仍待验收。
- 当前仍为未发布的 alpha.3 工作树，尚未提交、推送或部署。

## 2026-09-08 正文浏览器验收

- 新增 `test/fixtures/media.md` 原创样例，由主题 smoke 测试生成到临时站点，不进入正式博客。包含正常图片、失败图片、视频封面和失败源、宽表、长代码、公式、脚注、Mermaid。
- 内置 Chromium 实测桌面和 390px 手机正文：宽表局部滚动，长代码不撑宽页面，公式及 Mermaid 可见；修正样例图片重复添加子目录的问题，正常图片自然宽度为 960px，失败图片显示替代文本。
- 明暗切换、脚注跳转和返回、搜索结果均已操作确认。复制按钮显示“已复制”，未独立核验系统剪贴板内容。搜索页未记录控制台错误。
- 禁用 JavaScript 后正文、公式和脚注仍在，Mermaid 保留源码，不宣称无脚本图表渲染。
- 发现深色配色覆盖打印规则，已修复并通过打印媒体仿真确认白底黑字；未验收真实打印分页或 PDF。
- 真实首页 320px 视口下无页面横向溢出，键盘焦点轮廓可见。全页截图曾异常，改用可见区域截图后确认正常布局。
- 新样例加入后主题 15 项检查通过。仍待：真实视频播放、浏览器原生 200% 缩放、完整键盘遍历、资源失败重试，以及独立 Chrome/Edge/Firefox/Safari 验收。本轮结果不等于稳定版验收完成。

## 2026-09-08 安装、升级与回退复验

- `npm run test:lifecycle --prefix themes/breezehome -- D:/blog` 四阶段通过：当前工作树全新安装、alpha.2 基线、升级当前工作树、回退 alpha.2。
- 使用博客当前锁文件在独立临时目录执行离线 npm ci，各阶段清理缓存并重新构建；固定链接、正文和个人介绍检查通过，样例文章、配置、资料及依赖文件 SHA-256 保持不变。回退后 Mermaid 增强标记和供应商脚本消失，公式增强不再提供。
- 日志、主题快照和各阶段产物保留在命令输出的 breezehome-lifecycle-* 临时目录。npm 提示 hexo-util 安装脚本尚未列入 allowScripts；未修改许可设置，实际安装和构建成功。
- 同轮 smoke 17 项、博客构建和静态审计通过：268 个 HTML、3958 个内部引用、0 个缺失引用，图片 alt 和懒加载属性无缺失。
- 仅验证当前锁文件安装及主题切换，不代表远端克隆、无缓存联网安装或历史依赖迁移通过。原生 200% 缩放、打印分页、完整键盘流程和其他浏览器仍待补验；未提交、推送、打标签或部署。

## 2026-09-08 独立 Chrome 补充验收

- 新增本地生成的无声 WebM 动画样例，不访问摄像头、麦克风或外部视频。Chrome 中按空格开始播放，实际播放时间推进至 2.567655 秒并触发 ended，确认媒体解码和播放完成。
- 阻断 `search-index.json` 请求后显示加载失败提示；解除阻断，使用回车提交后恢复“找到 1 篇文章”。网络阻断已撤销。
- 修复“跳到正文”仅滚动但正文不接收焦点的问题，为 main 添加 `tabindex="-1"`；复验 activeElement 为 main。
- 长代码区新增键盘入口和区域名称，代码区、表格区和正文获得焦点时显示轮廓。640px 视口下，左右键实测表格及代码各滚动 40px；目录可用空格展开、回车跳转，复制可用回车触发成功提示。未以此替代完整 Tab 顺序和系统剪贴板验收。
- 增加 `test/preview.cjs`：只绑定本机回环地址，显式指定临时样例构建目录即可复现；命令 `node test/preview.cjs <npm test 输出的 public 路径>`。
- 当前连接能力只提供独立 Chrome 和内置 Chromium；Edge 连接返回不可用，未发现 Firefox 可执行文件，Safari 不在当前 Windows 验收环境中。
- 浏览器快捷键未实际改变页面缩放，也未打开可检查的打印预览。原生 200% 缩放和打印分页仍标为未验收，640px 布局检查不冒充 200% 缩放结论。这两项需有浏览器窗口控制能力的环境补验。
